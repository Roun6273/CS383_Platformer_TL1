using UnityEngine;

//A singleton so the level can be accessed from anywhere easily
public static class LevelSingleton {
	public static Level level;
	public static Player player;
	public static EndGoal endGoal;
}